name: checkpatch

on:
  push:
  pull_request:

jobs:

  checkpatch:
    name: Run checkpatch test
    runs-on: ubuntu-latest

    steps:
      - name: checkout gfarm
        uses: actions/checkout@v4
        with:
          repository: oss-tsukuba/gfarm
          path: gfarm
          #fetch-depth: 1

      - name: checkout gfarm2fs
        uses: actions/checkout@v4
        with:
          path: gfarm/gfarm2fs
          #fetch-depth: 0

      - name: invoke checkpatch
        id: checkpatch
        run: |
          set -x
          cd gfarm/gfarm2fs
          git diff ${{ github.event.before }} ${{ github.sha }} | \
            ../util/checkpatch/checkpatch.pl -
